# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api        # Base URL for backend API requests - points to the Express server's API endpoints
VITE_APP_NAME=Weather App                          # Application name used in titles, headers, and metadata throughout the React app
VITE_APP_VERSION=1.0.0                             # Current version of the application - used for display and cache busting

# Development Configuration
VITE_ENV=development                               # Vite environment mode - controls build optimizations and debugging features
VITE_DEBUG=true                                    # Enable debug mode for additional console logging and development tools

# Optional: Direct OpenWeather API access (for client-side calls if needed)
# VITE_OPENWEATHER_API_KEY=your_api_key_here      # Commented out OpenWeather API key for direct frontend API calls (not recommended for security - use backend proxy instead)